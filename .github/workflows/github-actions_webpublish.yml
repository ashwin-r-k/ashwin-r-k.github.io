name: GitHub Actions To publish Website
run-name: ${{ github.actor }} is Publishing or updating site GitHub Actions 🚀
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:
